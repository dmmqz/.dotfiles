backend = "glx";

# Opacity
active-opacity = 1;
inactive-opacity = 1;
frame-opacity = 1;

# Blurring
blur-background = false;

# Fading
fading = false;

# Shadow
shadow = false;

# Other
detect-rounded-corners = true;
detect-client-opacity = true;
vsync = true;
xrender-sync-fence = true;
